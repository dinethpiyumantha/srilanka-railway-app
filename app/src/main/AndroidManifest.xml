<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bugapocalypse.srilankarailwaydigital">
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/railway_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/railway_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SriLankaRailwayDigital">
        <activity android:name=".Qr_scanner" />
        <activity android:name=".before_admin_gif" />
        <activity android:name=".CreateUser" />
        <activity android:name=".UserLogin" />
        <activity android:name=".AboutUs" />
        <activity android:name=".DigitalTicket"/>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>